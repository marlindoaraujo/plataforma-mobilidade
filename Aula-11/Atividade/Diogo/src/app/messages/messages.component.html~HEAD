<div *ngIf="messageService.mensagens.length">
  <h2>Mensagens</h2>
  <div *ngFor='let message of messageService.mensagens'> {{message}} </div>
  <button (click)="messageService.clear()">Fechar</button>
</div>